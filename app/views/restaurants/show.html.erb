<h1>Restaurants#show mostar UN Restaurants</h1>

<% @restaurant.name %>
<table>
    <thead> 
        <tr>
            <th>nombre</th>
            <th>calificacion</th>
        </tr>
    </thead>    
    <tbody>
        <tr>    
            <td><%= @restaurant.address %></td>
            <td><%= @restaurant.rating %></td> 
        </tr>
    </tbody>
</table>
<%= link_to "regresar", restaurants_path %>
<%= link_to "editar", edit_restaurant_path %>
<%= link_to "borrar", restaurants_path(@restaurant), method: :destroy, data: {confirm: "¿Estas Seguro que quieres borrar el registro?"}  %>